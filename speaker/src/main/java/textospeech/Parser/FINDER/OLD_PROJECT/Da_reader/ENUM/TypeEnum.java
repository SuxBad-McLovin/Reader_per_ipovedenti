package textospeech.Parser.FINDER.OLD_PROJECT.Da_reader.ENUM;

/**
 * ENUM, utilizzata per definire all'utente il tipo di elemento
 */
public enum TypeEnum 
{
    NODATA,
    COMMENT, 
    STRING,
    CHAR,
    MULTI,                  // lui viene usato solo per un INTERVALS
    COMMENT_OPEN,           // loro due solo per POS
    COMMENT_CLOSE;
}
